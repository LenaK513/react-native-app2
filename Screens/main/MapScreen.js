import React from "react";
import { Text, StyleSheet } from "react-native";

const MapScreen = () => {
  return <Text style={styles.title}>MapScreen</Text>;
};

const styles = StyleSheet.create({
  title: {
    alignItems: "center",
  },
});

export default MapScreen;
