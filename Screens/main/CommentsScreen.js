import React from "react";
import { Text, StyleSheet } from "react-native";

const CommentsScreen = () => {
  return <Text style={styles.title}>CommentsScreen</Text>;
};

const styles = StyleSheet.create({
  title: {
    alignItems: "center",
  },
});

export default CommentsScreen;
