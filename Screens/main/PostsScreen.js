import React from "react";
import { Text, StyleSheet } from "react-native";

const PostsScreen = () => {
  return <Text style={styles.title}>PostsScreen</Text>;
};

const styles = StyleSheet.create({
  title: {
    alignItems: "center",
  },
});

export default PostsScreen;
